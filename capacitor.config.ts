import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nguoianphu.tabs',
  appName: 'tabs',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
